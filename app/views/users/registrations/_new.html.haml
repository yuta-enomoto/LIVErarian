.session__sns-2
  =link_to '#', class: 'face-book'do
    %i.fab.fa-facebook
    FACEBOOK で登録する
.session__sns-2.row
  =link_to '#', class: 'google' do
    %i.fab.fa-google
    GOOGLE で登録する

.session__line-3
  %strong.session__line__or
    または

.session__text-3
  メールアドレスで登録する。

= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
  -# = render "devise/shared/error_messages", resource: resource

  .session__field-2
    = f.text_field :nickname, placeholder: "ニックネーム", autofocus: true, autocomplete: "nickname", class: "form"
    
  .session__field-2
    = f.email_field :email, placeholder: "Eメールアドレス", autofocus: true, autocomplete: "email", class: "form"

  .session__field-2
    = f.password_field :password, placeholder: "パスワードを設定してください。", autocomplete: "new-password", class: "form"

  .session__field-2
    = f.password_field :password_confirmation, placeholder: "パスワードを再入力してください（確認用）。", autocomplete: "new-password", class: "form"
  
  .session__field-3
    %label.session__field-3__label 生年月日 
    .session__field-3__birthday
      != sprintf(f.date_select(:birthday,use_month_numbers:true, prompt:"--", start_year:Time.now.year, end_year:1920, date_separator:'%s'),'年','月')+'日'
      %i.fas.fa-chevron-down.bar-1
      %i.fas.fa-chevron-down.bar-2
      %i.fas.fa-chevron-down.bar-3

  .session__recaptcha
    = recaptcha_tags

  .session__actions
    = f.submit "登録する", class: "register-btn"

.session__go-login
  アカウントを持っている。 → 
  = link_to new_user_session_path do
    ログイン


.session__line-2
  .session__line-2__texts-2
    登録する際は、LIVErarianの
    = link_to "#" do
      利用条件
    および
    = link_to "#" do
      プライバシーポリシー
    をご確認ください。
    また、FacebookまたはGoogleで登録された場合においても、LIVErarianの
    = link_to "#" do
      利用条件
    および
    = link_to "#" do
      プライバシーポリシー
    に同意したものと見なされます。


