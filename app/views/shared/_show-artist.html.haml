.show-main__container__top
  .show-main__container__top__left
    = link_to artist_path(@artist.id) do 
      = image_tag @artist.image.url

  .show-main__container__top__right
    %h2.show-main__container__top__right__name
      = link_to artist_path(@artist.id) do 
        = @artist.name
    %h2.show-main__container__top__right__text
      = link_to posts_artist_path(@artist.id) do
        開催予定のLIVE (
        = @count_yet
        件)
    %h2.show-main__container__top__right__text
      = link_to done_posts_artist_path(@artist.id) do
        過去に開催したLIVE (
        = @count_done
        件)

    - if user_signed_in? && current_user.id == @artist.user.id
      = link_to edit_artist_path(@artist.id), class: "edit" do
        アーティスト情報を編集

    - elsif user_signed_in? 
      %h2.show-main__container__top__right__follow
        フォロー